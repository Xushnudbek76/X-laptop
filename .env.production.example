PORT=3003
MONGO_URI=mongodb://mongo:27017/x-laptop
SESSION_SECRET=change-me-to-a-long-random-session-secret
SECRET_TOKEN=change-me-to-a-long-random-jwt-secret

# Use this if your frontend is built separately against the backend host.
# The bundled CORS config currently reflects the request origin.
# FRONTEND_URL=https://example.com
